import {fetchState, fetchChildState, createDefaultChildStorageKey, createStorageKey, Hasher} from "./fetch_state";

export default {
    fetchState,
    fetchChildState,
    createDefaultChildStorageKey,
    createStorageKey,
    Hasher
}

export {fetchState, fetchChildState, Hasher, createStorageKey, createDefaultChildStorageKey};