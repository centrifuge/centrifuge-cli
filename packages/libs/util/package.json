{
  "name": "@centrifuge-cli/util",
  "version": "0.1.0",
  "license": "Apache-2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "directory": "packages/libs/util",
    "url": "https://github.com/centrifuge/centrifuge-cli.git"
  },
  "scripts": {
    "clean": "rimraf dist node_modules tsconfig.tsbuildinfo .nyc_output",
    "prepack": "yarn build",
    "build": "yarn clean && yarn compile",
    "compile": "tsc --build && cp \"./package.json\" ./dist/",
    "test": "echo 'no test'",
    "lint": "eslint \"./src/**/*.{ts,tsx}\" --max-warnings=0"
  },
  "devDependencies": {
    "rimraf": "^3.0.0"
  }
}